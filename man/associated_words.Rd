\name{associated_words}
\alias{associated_words}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
from the table of ngrams generated by ngram_clean() find the most common associated words by typology
}
\description{
from the table of ngrams generated by ngram_clean() find the most common associated words by typology
}
\usage{
associated_words(clean_ngram, type_word, min_occurence = 5)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{clean_ngram}{
output from ngram_clean()
}
  \item{type_word}{
the name of the category to analyse (check outptu of ngram_clean() to see the available list)
}
  \item{min_occurence}{
only display the words with at least this specified number of occurence
}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
a tibble with ngrams in rows, number of occurence and dummies for the typologies 
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
%%  ~~who you are~~
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{

function (clean_ngram, type_word, min_occurence = 5) 
{
    dt = clean_ngram \%>\% filter(get(type_word) == 1) \%>\% separate(word, 
        "list_nwords", sep = " ") \%>\% gather() \%>\% filter(value != 
        "") \%>\% group_by(value) \%>\% summarize(n = n()) \%>\% filter(!str_detect(value, 
        "\\d")) \%>\% arrange(-n) \%>\% rename(word = value)
    dt = dt \%>\% filter(!is.na(word) & word != "" & n > min_occurence)
    return(dt)
  }
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }% use one of  RShowDoc("KEYWORDS")
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
